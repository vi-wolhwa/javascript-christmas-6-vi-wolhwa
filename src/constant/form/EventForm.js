const EVENT_FORM = (name, type, condition, benefits, description) => {
  return {
    name: name,
    type: type,
    condition: condition,
    benefits: benefits,
    description: description
  }
};

export { EVENT_FORM };
